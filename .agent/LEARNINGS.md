# Learning Inbox

Temporary review queue for non-obvious findings that are not yet classified as memory,
decision, known trap, automation, or skill candidate.

- 2026-09-12: Redesigned admin model settings (`src/components/model-settings-page.tsx`) from 3-tab layout to provider-card grid + per-provider models modal. Provider/model CRUD unchanged (same `/api/admin/model-settings/*` endpoints; `ModelInputSchema` has zod defaults for omitted fields like `isCurator`). Note: `ui/model-provider-icon.tsx` previously matched only `"openRouter"` (camelCase) while `ProviderTypeSchema` uses `"openrouter"`; fixed to accept both. Admin UI pages use hardcoded English strings (not next-intl) — keep consistent.

- 2026-09-12: System model engines now carry a `configurable` flag (`src/lib/ai/system-model-engines.ts`). `thread-title` and `context-summary` are non-configurable: hidden from admin settings (`getSystemModelEngineSettings` filters them), DB assignments are ignored at resolution time (always inherit), and the title API inherits the caller's selected `chatModel` from the request body with fallback to the global default. Auxiliary engines have no capability requirements; background/vector engines (memory-curator, automation/delegation-runner, memory-embedding) must stay admin-configured.

- 2026-09-12: Memory embedding subsystem fully removed (`0072_drop_memory_embedding.sql` drops `memory_embedding`, `is_embedding_default`/`embedding_dimensions` columns, and stale `memory-embedding` engine assignments). Recall is lexical-only: shared helper `src/lib/ai/memory/lexical-search.ts` provides `lexicalTerms/contentMatches/contentRank` used by both memory repos — `ts_rank_cd` + OR-joined prefix tsqueries (`term:*`, compensating for the stemmer-less 'simple' config) + pg_trgm fuzzy fallback with graceful degradation when the extension is missing. `memory.recallMode` setting and `getMemoryRecallMode` were deleted; integration suite runs against a disposable database only (harness wipes the target DB).
