FROM node:22-alpine

ENV NODE_ENV=production \
  HOST=0.0.0.0 \
  PORT=8788
WORKDIR /broker

RUN addgroup -S -g 10001 broker \
  && adduser -S -D -H -u 10001 -G broker broker

COPY --chown=broker:broker docker/sandbox/package-broker ./

USER broker
EXPOSE 8788

CMD ["node", "server.mjs"]
